extends State

func _ready() -> void:
	state_name = 'carrying'

# Relinquish control to the player!!!


#func physics_process(delta: float) -> void:
	## Keep the helipod on the player. 
	#actor.global_position = Vector2(actor.player.global_position.x, actor.player.global_position.y-30)
